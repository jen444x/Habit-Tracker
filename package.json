{
  "scripts": {
    "tailwind": "tailwindcss --input habit_tracker/static/css/tw.css --output habit_tracker/static/css/tailwind.css --watch",
    "browser-sync": "browser-sync start --proxy localhost:8000 --files 'habit_tracker/templates/**/*.jinja, habit_tracker/static/**/*' --no-ghost-mode",
    "dev": "npm run tailwind & npm run browser-sync"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.17",
    "browser-sync": "^3.0.4",
    "tailwindcss": "^4.1.17"
  },
  "dependencies": {
    "matplotlib": "^1.0.0",
    "pandas": "^0.0.3"
  }
}
